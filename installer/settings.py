# install.py by Hudson Smith.
class Settings(object):
    # What the virtual environment should be named.
    venv_name = "env"

    # The path to python within the environment.
    python_venv_path = f"{venv_name}/bin/python"

    requirements_file = "requirements.txt"
